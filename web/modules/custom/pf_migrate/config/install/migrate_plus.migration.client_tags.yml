id: client_tags
migration_group: phil_frilling
migration_tags: {}
label: 'Taxonomy - Client Tags'

source:
  plugin: client_tags
  key: legacydb
  high_water_property:
    name: changed

destination:
  plugin: 'entity:taxonomy_term'

process:
  type:
    plugin: default_value
    default_value: client_tags
  name: name
#  tid: tid
  uuid: uuid
  langcode: langcode
  weight: weight
  changed: changed
  default_langcode: default_langcode
  status: status
  revision_translation_affected: revision_translation_affected
  vid:
    plugin: default_value
    default_value: client_tags
  'description/value': description__value
  'description/format':
    plugin: default_value
    default_value: full_html

dependencies:
  module:
    - pf_migrate
